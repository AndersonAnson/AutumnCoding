package array;

public class MergeInterval {
    public int[][] merge(int[][] intervals) {
        int intervalsNum = intervals.length;
        int len = intervals[0].length;
        if (intervalsNum == 1) {
            return intervals;
        }
        return intervals;
    }
}
